<Window x:Class="CommandTest.ResponseTimeDistributionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommandTest"
        xmlns:ScottPlot="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
        Title="応答時間分布" Height="450" Width="800" WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- グラフ表示領域 -->
        <ScottPlot:WpfPlot Grid.Row="0" x:Name="PlotControl"/>
        
        <!-- 操作部 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,5,5">
            <StackPanel Orientation="Horizontal" Margin="0,0,5,0" VerticalAlignment="Center">
                <TextBlock Text="バー数:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="BinCountComboBox" Width="60">
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="10"/>
                    <ComboBoxItem Content="15"/>
                    <ComboBoxItem Content="20"/>
                    <ComboBoxItem Content="25"/>
                    <ComboBoxItem Content="30"/>
                </ComboBox>
            </StackPanel>
            <Button Content="更新" Width="80" Margin="5,0" Click="UpdateButton_Click"/>
            <Button Content="閉じる" Width="80" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
