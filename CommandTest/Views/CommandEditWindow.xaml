<Window x:Class="CommandTest.Views.CommandEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommandTest.Views"
        mc:Ignorable="d"
        Title="コマンド編集" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- コマンド設定部 -->
        <GroupBox Grid.Row="0" Header="コマンド設定">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左側：コマンド入力エリア -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBox Grid.Row="0"
                             x:Name="CommandTextBox"
                             Margin="5"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"/>
                    
                    <Button Grid.Row="1"
                            Content="制御記号挿入"
                            HorizontalAlignment="Right"
                            Margin="5"
                            Padding="10,3"
                            Click="InsertControlCharacter_Click"/>
                </Grid>

                <!-- 右側：各種設定 -->
                <StackPanel Grid.Column="1" Margin="5">
                    <GroupBox Header="送受信モード">
                        <ComboBox x:Name="ModeComboBox"
                                Width="120"
                                Margin="5"
                                SelectedValuePath="Tag">
                            <ComboBoxItem Tag="Normal">
                                <TextBlock Text="通常モード"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="NoCommand">
                                <TextBlock Text="受信のみ"/>
                            </ComboBoxItem>
                            <ComboBoxItem Tag="NoResponse">
                                <TextBlock Text="送信のみ"/>
                            </ComboBoxItem>
                        </ComboBox>
                    </GroupBox>
                    <GroupBox Header="タイムアウト(ms)">
                        <TextBox x:Name="TimeoutTextBox"
                               Width="120"
                               Margin="5"/>
                    </GroupBox>
                    <GroupBox Header="待ち時間(ms)">
                        <TextBox x:Name="IntervalTextBox"
                               Width="120"
                               Margin="5"/>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- ボタン部 -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Margin="5,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="キャンセル" 
                    Width="80" 
                    Margin="5,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
